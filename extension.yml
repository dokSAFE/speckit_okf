# Spec Kit extension manifest
# Docs: https://github.com/github/spec-kit/blob/main/extensions/EXTENSION-DEVELOPMENT-GUIDE.md
schema_version: "1.0"

extension:
  id: "okf"
  name: "OKF Knowledge Bundle Generator"
  version: "0.6.0"
  description: >
    Generates and maintains an Open Knowledge Format (OKF v0.1) knowledge
    bundle from a source-code repository. Adds commands to bootstrap a
    bundle (mining git history for the "why"), incrementally update it,
    clarify open questions with the user, validate conformance against
    the OKF spec, and verify that the bundle's claims are actually
    supported by the repository
    (https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md).
  author: "Alex Punnen <alexcpn@gmail.com>"
  repository: "https://github.com/alexcpn/speckit_okf.git"
  homepage: "https://github.com/alexcpn/speckit_okf"
  license: "MIT"
  tags: [knowledge, okf, documentation, metadata, catalog]
  category: "documentation"
  effect: "read-write"   # creates/modifies files under the knowledge bundle dir

requires:
  speckit_version: ">=0.12.0"

# scripts/bash/ and scripts/python/ are plain supporting files, not
# manifest-declared assets — the extension installer copies this whole
# directory tree to .specify/extensions/okf/, and the command files below
# reference the scripts by that hardcoded path. If you move or rename the
# scripts directory, update all command files to match.
provides:
  commands:
    - name: "speckit.okf.generate"
      file: "commands/generate.md"
    - name: "speckit.okf.update"
      file: "commands/update.md"
    - name: "speckit.okf.clarify"
      file: "commands/clarify.md"
    - name: "speckit.okf.validate"
      file: "commands/validate.md"
    - name: "speckit.okf.verify"
      file: "commands/verify.md"
  config:
    - name: "okf-config.yml"
      template: "okf-config.template.yml"
      required: false
